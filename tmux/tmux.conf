set -g terminal-features "tmux-256color:RGB"
set -g prefix C-s
set -g base-index 1
set -g renumber-windows on
set -g mode-keys vi
set -g status-position top
set -g mouse on

set -g status-justify absolute-centre
set -g status-style "bg=default"
set -g window-status-current-style "fg=blue bold"
set -g window-status-current-format "#I:#W"
set -g window-status-format "#I:#W"
set -g status-right ""
set -g status-left "#S"
set -g status-right-length 20
set -g status-left-length 20
set -g pane-active-border-style "fg=blue"
set -g pane-border-style "fg=black"

bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind r source ~/.config/tmux/tmux.conf
bind f run "tmux neww ~/.config/tmux/scripts/tmux-session-dispensary.sh"
bind H run "~/.config/tmux/scripts/tmux-session-dispensary.sh ~"
bind C run "~/.config/tmux/scripts/tmux-session-dispensary.sh ~/.config"
bind P run "~/.config/tmux/scripts/tmux-session-dispensary.sh ~/Documents/Projects/"
bind G neww -n "lazygit" lazygit
bind E show-environment -g

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
